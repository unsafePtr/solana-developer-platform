/**
 * Compatibility entry point retained for generated clients that import it for
 * side effects. Kit 6.10 exports `ExtendedClient` natively, so the former module
 * augmentation must remain disabled to avoid a duplicate type declaration.
 */
export {};
